import CTA from '@/section/CTA';
import FAQ from '@/section/FAQ';

const HomePage = () => {
    return (
        <main>
            <FAQ />
            <CTA/>
        </main>
    );
};

export default HomePage;